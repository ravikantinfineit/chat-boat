# Where the NestJS API is reachable from the browser.
VITE_API_BASE_URL=http://localhost:3000

# The showroom's public widget key, shown in the admin panel after you connect
# a system. Public by design — it identifies the tenant and never reaches the ERP.
VITE_WIDGET_KEY=
